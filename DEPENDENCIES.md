# Dependencies

- **Manifest:** none. No package manager and no runtime dependencies.
- **Contents:** static binary assets versioned directly in git.
- **Supply chain:** an SBOM workflow (`.github/workflows/sbom.yml`) runs in CI. Automated dependency updates (Dependabot): not applicable (no manifest).
